# Mixxx Database Terminal UI

Use it to quickly query your Mixxx database, to copy artist + title to your clipboard, or to play a file using mpv.

## Installation

```bash
go install github.com/pbleser/qmixxx@latest
```
